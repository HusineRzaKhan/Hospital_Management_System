import 'package:flutter/material.dart';

// Manager role removed from the project.
// This placeholder keeps the file valid in case any imports remain elsewhere.
class ManagerSignup extends StatelessWidget {
  const ManagerSignup({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Manager (removed)')),
      body: Center(child: Text('Manager role has been removed.')),
    );
  }
}
