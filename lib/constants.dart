import 'package:flutter/material.dart';

class AppColors {
  static const Color backgroundColor = Color(0xFFBCCBE3); // Background color
  static const Color primaryColor = Color(0xFF4287F5); // Primary color
  static const Color accentColor = Color(
      0xFF3A5EBC); // Accent color (optional, can be used for buttons, highlights, etc.)
}
